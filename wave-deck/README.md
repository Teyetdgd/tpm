# Wave Deck

A LumiaSDK WAV player demonstrating the shared kernel audio engine, GUI widget
API, seek bar, transport buttons, and volume slider. Pass a VFS `.wav` path as
the first argument; the default is `test.wav`.

```bash
make LUMIA_SDK=/path/to/Lumia-OS/sdk/toolchain
```
