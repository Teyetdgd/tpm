# Pocket Notes

A compact graphical note editor written only against LumiaSDK. It loads and
saves `pocket-notes.txt` in the current VFS directory and demonstrates keyboard
events plus guest-wide clipboard copy and paste.

```bash
make LUMIA_SDK=/path/to/Lumia-OS/sdk/toolchain
```
